# FIGMA_TOKEN is an OPTIONAL dev fallback.
# The normal way to set the Figma token is via the in-app Settings modal
# (persisted to <userData>/settings.json; never logged or returned to the renderer).
# Only set this here if you need it as a fallback during local development.
# FIGMA_TOKEN=your-figma-personal-access-token
